# Memory — 2026-08-17

## What we built: `scraper` module (Nepal Supreme Court hearing tracking)

New Spring Boot module `com.lawfirm.erp.modules.scraper` — scrapes supremecourt.gov.np
cause lists, matches them against our client cases, and notifies on new hearings.

### Why plain HTTP, not Selenium
The site is server-rendered PHP forms returning full HTML on POST — Selenium would add a
browser for no benefit. Used `java.net.http.HttpClient` + jsoup (added to pom) behind a
`CourtSiteClient` interface so a browser-backed impl can be swapped in later if the site
ever gates on JS. The user's own spec (httpx) was plain HTTP too.

### Verified live site mechanics (today)
- GET the endpoint first → server issues PHPSESSID + `court_session` + F5 `TS...` cookie.
  `court_session` binds the USER_AGENT → must send a consistent browser-like UA.
- POST → **302** with a flash in `court_session` (e.g. "causelist not published") → follow
  Location with GET. Java HttpClient does NOT follow 302 on POST → manual follow in
  `HttpCourtSiteClient`.
- **Daily** form: `todays_date` (Arabic BS) + `pesi_date` (Devanagari BS) + `submit=खोज्नु होस्`.
  Daily pages for past dates return "Causelist is not available" — today's isn't published
  until later morning. Daily = 10-col rows, per-judge इजलाश sections, separate वादी/प्रतिवादी,
  has आदेश फैसलाको किसिम (outcome) column.
- **Weekly** form: `pesi_bar=all` (day-of-week select, NOT pesi_date) + submit. Returns the
  whole Sun–Fri week (~1.7 MB), grouped by `पेशी मिति :<date>`, flat tables, single
  `पक्ष || विपक्ष` column split on `||`/`‖`, **no** outcome column.
- Case numbers come in two forms per cell: `०८१-C४-३८२७` (court-visible) and
  `(३९-०८१-३२०३०)` (court-scoped internal id). Both extracted + normalized to Arabic.
- Court ids: 39 = Kathmandu, 63 = Gulmi; consistent across all 4 tab endpoints.

### BS ↔ AD conversion
`NepaliDateUtil` — month-length table for BS 2000–2090 (91 years, extracted from the
`nepali-date-converter` npm package 3.4.0), epoch BS 2000-01-01 = AD 1943-04-13,
AD = 1943-04-13 + daysPassed. Verified: 2081-01-01 → 2024-04-13, 2083-05-01 → 2026-08-17.
`DevanagariConverter` for ०-९ ↔ 0-9.

### Module layout
- `entity/`: Court (courtId PK, standalone — NOT auditable, else double-@Id), ClientCase
  (courtId + caseNoInternal, ACTIVE drives the court list), DailyHearing / WeeklyHearing
  (unique key courtId+caseNoInternal+hearingDateBs → idempotent upsert), HearingMatch.
- `parser/`: DailyTableParser (10-col, skips summary table, judge from इजलाश heading),
  WeeklyTableParser (9-col, date headers, party split), CaseNumberExtractor.
- `client/`: CourtSiteClient interface + HttpCourtSiteClient (session, 302 follow, rate limit).
- `service/`: HearingIngestionService (upsert), HearingMatchingService (join client_cases ×
  hearings on courtId+caseNoInternal, dedupe, at-least-once notify), NotificationDispatcher
  interface + LogNotificationDispatcher stub, ScraperService (orchestrator: active courts →
  concurrent fan-out on bounded executor → ingest → match+notify; alerts on failure/0 rows),
  ScraperScheduler (@Scheduled 10:05 weekdays / 10:15 Mon, `scraper.daily-cron` etc.).
- `controller/`: GET `/api/v1/cases/{caseNo}/hearing-status` (DB only), POST
  `/api/v1/scraper/admin/scrape?courtId=&date=` (FIRM_ADMIN).
- `config/`: ScraperProperties (`scraper.*`), SchedulingConfig (`@EnableScheduling` — this
  ALSO activated the previously-dormant AppealDeadlineEngine cron; documented in its javadoc).

### Tests (75 total green, 24 new)
Converters, both parsers against REAL fixtures (weekly_kathmandu_39.html captured live today,
daily fixture from user's real pasted rows), ingestion idempotency, matching dedupe +
notification decoupling (failing dispatcher never blocks matching), orchestrator
(fan-out/failure/0-row/disabled), DB-only read path.

### Live smoke test (today, real site)
Real client + real parser: weekly scrape of court 39 → 1,721 parsed records, correct
BS+AD dates and both case-number forms. ✓

### Gotchas for tomorrow
- `-Dtest='scraper.*'` doesn't match by package — use class names or run the whole suite.
- Windows classpath separator is `;` not `:` when compiling standalone mains.
- Git Bash `grep -E "[ \t]+$"` is unreliable (`\t` not interpreted) — use `[[:blank:]]+$`.
- Node on Git Bash resolves `/tmp` to `G:\tmp` — copy files into the project for node scripts.
- The daily feed for a given date is often "not published" in the morning — 0 rows is
  expected, not an error; the orchestrator logs it as an alert.

### Late correction — the REAL daily structure (user supplied the actual response)
The real daily page's judge sections are NOT `h3` headings (as first assumed) — they are
**plain header tables** with `<td><b> इजलाश N</b></td>` + `<td class="judge"><b> माननीय
जिल्ला न्यायाधीश श्री …</b></td>`, followed by a `record_display` detail table
(10 cols). DailyTableParser rewritten to extract the judge NAME from `td.judge` and the
page date from the "मिति <date> को दैनिक पेशि सुची" title. Verified against the real
660 KB response: **672 rows = exactly the site's own जम्मा total**, 30 judges, 72 rows
with outcomes (स्थगित/आदेश/फैसला). Fixture `daily_kathmandu_39.html` is now the real file.

### CSV weekly export (decided + implemented today, after the archive brainstorm)
User chose: DB stays system of record; raw rows kept forever; matches = enriched working
set; weekly CSV snapshot (not xlsx — no new dependency, BOM + quoting needed for Devanagari
in Excel). Implemented:
- HearingMatch enriched: +caseNoBs, subject, plaintiff, defendant, caseNoInternal, orderType
  (full row copied at match time) + startup backfill for legacy rows.
- HearingExportService: writes week-<monday>_<sunday>.csv (previous Mon-Sun window,
  daily+weekly merged, UTF-8 BOM, all fields quoted, sorted by court then date, `matched`
  Y/N column, temp-file + rename so no half-written files). Cron Mon 10:30
  (scraper.export-cron), POST /api/v1/scraper/admin/export manual trigger.
- scrapper.export-dir config (default ./exports).
- Tests: HearingExportServiceTest (3) + backfill test. Worktree 79/79 green, devG 109/109.
- Docs: docs/scraper-module.html + scraper-module.pdf (register_preview to view).

### Date-filtered hearing lookup (user asked "search a case on a specific date")
The scrape admin endpoint pulls ANY date's list into the DB (e.g. date=2083-04-29). But
the read endpoint had no date filter — added optional `?date=` (BS yyyy-mm-dd) to
GET /api/v1/cases/{caseNo}/hearing-status. Service now queries
findByCaseNoInternalAndHearingDateBs on both daily+weekly repos when date given, else the
full order-by-date query. Test added (ScraperServiceTest#hearingStatusFilteredByDate).
Worktree 79/79, devG 109/109 (both excluding ErpApplicationTests — it needs the secret
`config.encryption.key` env var, only set in the user's normal shell; unrelated to scraper).

### Schema overhaul + weekly mode (user supplied real daily+weekly HTML)
User pasted a table they thought was weekly (इजलाश benches + 10 cols + outcome) — it is the
DAILY structure. Their real Weekly.html (1.7MB) confirms: 0 इजलाश, 0 judge headers, 0 outcome
col — weekly is per-date (पेशी मिति :date), 9 cols, combined पक्ष‖विपक्ष. Implemented:
- All scraper entities now standalone: Long IDENTITY id (no UUID), no audit fields
  (created/updated/by), + scrapedDate (LocalDate). Bench (इजलाश, daily only) + serialNo
  (क्र. स., Devanagari->Arabic, letters क/ख stay) added to DailyHearing/WeeklyHearing/HearingMatch.
- DailyTableParser extracts bench number via BENCH_HEADER regex + serial from cell 0;
  WeeklyTableParser extracts serial from cell 0 (bench stays null).
- Admin scrape endpoint now takes mode=daily|weekly:
  POST /api/v1/scraper/admin/scrape?courtId=39&mode=weekly (+ ScraperService.runWeeklyScrapeForCourt).
- Export CSV header now includes bench,serialNo after caseNoInternal.
- Live smoke (real site): weekly 1721 rows serial populated bench null; daily 703 rows =
  exactly the site's own जम्मा: ७०३ total, all rows bench, 495 serial.
- Worktree 79/79 (excl. ErpApplicationTests), devG 109/109. Repos changed UUID->Long.

## Code style pass (comment cleanup)
- Stripped verbose class-level javadocs across the scraper module per code-style skill; kept only short comments for non-obvious logic (upsert keys, session flow, weekly-vs-daily schema differences, Devanagari conversion).
- Controllers keep @Tag/@Operation Swagger annotations (API docs, not comments).
- Trimmed final verbose javadocs in Court.java and HearingRecord.java to single-line comments.
- Verified: worktree 29/29 scraper tests, devG 29/29, both BUILD SUCCESS; trees byte-identical.

## Live case search (tab3 case_process_detail) implemented
- Verified live: POST /weekly_dainik/pesi/case_process_detail/{courtId} with mudda_no=<DISPLAY form e.g. 081-C1-7530> + submit=खोज्नु होस् (submit field is REQUIRED; internal form 39-081-39885 returns "भेटिएन").
- CourtSiteClient.scrapeCaseDetail(courtId, caseNoBs) added.
- CaseDetailParser: h2 numbers (मुद्दा <no>-बिवरण, रजिष्ट्रेशन नं), dl info block, तारेख विवरण, वादी/प्रतिवादी tables, पेशी विवरण (judge + order). "भेटिएन" → found=false.
- New endpoint: GET /api/v1/cases/live-detail?courtId=39&caseNo=081-C1-7530 (roles FIRM_ADMIN/ADVOCATE/PARALEGAL).
- Fixtures case_detail.html (real capture) + case_detail_notfound.html; tests: CaseDetailParserTest (2) + ScraperServiceTest.liveDetail.
- Gotchas hit: प्रतिवादीहरु contains वादीहरु substring (check प्रतिवादी first); Devanagari digits in division/hearingCount need toArabic; fixture uses U+200C ZWNJ not U+200D.
- Worktree 32/32, devG 32/32, BUILD SUCCESS; live smoke test passed (found=true, 1 plaintiff, 1 defendant, 7 tarekhe, 6 hearings).
