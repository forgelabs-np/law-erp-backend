# 2026-09-12

## RBAC Simplification — Template Layer Removed

- **Removed entire template/sync infrastructure**: `TemplatePermissionService`, `TemplateSyncPlanner`, `TemplateSyncRunner`, `FirmSyncExecutor`, `SyncJob` entity+repo, 4 DTOs, 4 controller endpoints, all cascade logic (~900+ lines deleted)
- **Simplified Role entity**: removed `parentRoleId`, `lastSaEditAt`, `extendsRole` fields
- **New ceiling model**: FIRM_ADMIN has no ceiling (SA sets via override), other roles ceilinged by FIRM_ADMIN's enabled permissions
- **Simplified FirmRoleServiceImpl**: removed cascade logic, `computeCeiling()` now returns all non-GLOBAL permissions for FIRM_ADMIN, firm FIRM_ADMIN's perms for other roles
- **Simplified RolePermissionServiceImpl**: removed parent_role_id ceiling check, only GLOBAL scope check remains
- **Cleaned up**: DataInitializer (removed backfillCustomRoleParentIds + seeder freeze), RoleController (removed template endpoints), RoleManagementService (removed getSystemTemplates), RbacConstants/SuperAdminConstants (removed template constants), AuditAction (removed TEMPLATE_PERMISSION_CHANGED)
- **8 new E2E tests** in `SimplifiedRbacE2ETest`: full SA→Firm→Ceiling→FirmAdmin→Employee flow, multi-firm isolation, SA custom role on-behalf, ceiling enforcement
- **325 tests, 0 failures** — all existing + new tests pass

## Key Design Decisions
- FIRM_ADMIN ceiling = no ceiling (all non-GLOBAL permissions available)
- Other roles ceiling = firm FIRM_ADMIN's enabled permissions
- SA sets FIRM_ADMIN permissions via `PUT /super-admin/firms/{firmId}/roles/{roleId}/permissions`
- Firm Admin distributes subset to employee roles via `PUT /firm/roles/{roleId}/permissions`
- Per-firm isolation: Firm A and Firm B are completely independent
- Custom roles: SA can create on-behalf, Firm Admin can create within firm

## Dashboard Firm Count Fix
- `FirmStats.totalFirms` now uses `countFirmsWithFirmAdmin()` (firms with at least one FIRM_ADMIN user) instead of `firmRepository.count()`
- Added `countFirmsWithFirmAdmin()` and `countActiveFirmsWithFirmAdmin()` to FirmRepository

## Enable Module Simplification
- `EnableModuleRequest` now only contains `moduleId` + `isEnabled` (removed `trialDays`, `maxFileSizeMb`, `allowedExtensions`, `notes`)
- Cleaned up `FirmModuleServiceImpl.enableModuleForFirm()` — no more trial/extension/validation logic in module enable

## Trial Period Feature
- **Firm entity**: added `isTrial`, `trialDays`, `trialStartedAt`, `trialExpiresAt` columns
- **CreateFirmRequest**: added `isTrial` + `trialDays` — SA sets trial when creating firm
- **FirmServiceImpl.createFirm()**: sets trial fields, `FirmStatus.TRIAL` if trial, `ACTIVE` if not
- **New endpoints** on `FirmAdminController`:
  - `PUT /{firmId}/suspend` — blocks all firm users
  - `PUT /{firmId}/activate` — reactivates (restores TRIAL if still valid)
  - `PUT /{firmId}/extend-trial` — pushes `trialExpiresAt` forward by `additionalDays`
  - `PUT /{firmId}/convert-to-permanent` — clears trial fields, sets `ACTIVE`
- **MeResponse.FirmInfo**: added `status`, `isTrial`, `trialExpiresAt`, `daysRemaining`
- **MeServiceImpl.getMe()**: gates on suspended firm (throws ForbiddenException) and trial expiry (returns empty modules + permissions)
- **TrialExpiryScheduler**: daily 09:00 cron, sends `TRIAL_EXPIRING` (3 days before) and `TRIAL_EXPIRED` notifications to FIRM_ADMIN role, auto-suspends expired firms
- **NotificationType**: added `TRIAL_EXPIRING` (ALERT), `TRIAL_EXPIRED` (SYSTEM)
- **NotificationRenderer**: renders trial expiry/expired templates
- **344 tests, 0 failures** — all existing + new tests pass

## Postman Collection Updated
- `docs/postman/RBAC-V3.postman_collection.json` updated with:
  - `isTrial` + `trialDays` fields in Create Firm requests
  - New `Create Trial Firm (15 days)` request
  - `Get Me` request (shows trial status in response)
  - New folder **5. Firm Lifecycle (SA)**: Suspend, Activate, Extend Trial (7 days), Convert to Permanent
  - Firm B creation updated with `isTrial: false`

## Code Review Minor Fixes
- `FirmStatus.EXPIRED` — documented as reserved/legacy (scheduler auto-suspends instead)
- `convertToPermanent` — added comment explaining trial fields kept for audit trail
- `TrialExpiryScheduler` — replaced hardcoded `"FIRM_ADMIN"` with `RoleCode.FIRM_ADMIN` constant
- `NotificationRenderer.renderTrialExpiring` — added try-catch around `Integer.parseInt` to handle missing/malformed `daysRemaining` variable
