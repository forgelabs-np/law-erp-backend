# 2026-08-21

## N+1 Query Optimization
- Scanned all 34 entity classes, 34 repositories, and 26 service implementations for N+1 patterns
- Found and fixed 7 N+1 hotspots:

### Fixes Applied
1. **EmployeeServiceImpl.getAllEmployees()** — `findByUserId` in loop → batch `findAllByUserIdIn`
2. **CourtEventServiceImpl.toResponse()** — `findById` per event in `listEvents()` → batch-resolved matter once
3. **CourtCaseServiceImpl.listUpcomingAppealDeadlines()** — `existsByParentCourtCaseId` per case in filter → batch `findParentIdsWithChildren`
4. **UserManagementServiceImpl.bulkDeactivate()** — `findById` per user → batch `findAllById`
5. **UserManagementServiceImpl.bulkRoleChange()** — `findById` per user → batch `findAllById`
6. **FirmRoleServiceImpl.updateRolePermissions()** — `findById` per permission in ceiling check → batch `findAllById` first
7. **CourtEventServiceImpl** — Added overloaded `toResponse(event, cc, matter)` to avoid redundant matter lookups

### New Repository Methods Added
- `EmployeeProfileRepository.findAllByUserIdIn(List<UUID>)` — batch profile load
- `CourtCaseRepository.findParentIdsWithChildren(Collection<UUID>)` — batch child check

### Tests Updated
- `CourtCaseServiceTest` — updated mock from `existsByParentCourtCaseId` to `findParentIdsWithChildren`
- `UserManagementServiceTest` — updated mock from `findById` to `findAllById`

All 132 tests pass, 0 failures, 0 errors.

---

## Project Management Module — Full Implementation
- Designed and built entire module from brainstorming through implementation
- 6 entities, 6 repositories, 6 service interfaces + impls, 6 controllers
- Design spec at `docs/project-management-design.md`

### Entities Created
- **Project** (UUID) — aggregate root, linked to client user
- **Credential** (Long) — encrypted portal logins per project
- **RenewalType** (Long) — shared lookup (system + firm custom)
- **Renewal** (Long) — recurrence template
- **RenewalInstance** (Long) — each deadline occurrence
- **ProjectMember** (Long) — owner + team assignments

### Key Features
- AES-256-GCM encrypted credentials (reuses ConfigEncryptionUtil)
- Recurrence engine: ONE_TIME, YEARLY, QUARTERLY, MONTHLY auto-generates instances
- Daily scheduler marks overdue instances
- Client portal: limited view (no credentials)
- RBAC: PROJECT_MANAGEMENT module with 6 permissions (VIEW, CREATE, EDIT, DELETE, CREDENTIAL_VIEW, CREDENTIAL_REVEAL)

### Files Created/Modified
- 6 enums, 6 entities, 6 repositories, 9 request DTOs, 9 response DTOs
- 1 mapper, 6 service interfaces, 7 service impls (incl. RenewalScheduler), 6 controllers
- Constants file, DataInitializer updated (module + permissions + default renewal types)
- AuditAction/AuditEntity enums extended
- PermissionAction enum extended (CREDENTIAL_VIEW, CREDENTIAL_REVEAL)
- ApiResponse class gained static `success()` factory methods
- 2 test classes (ProjectServiceTest, RenewalServiceTest)

All 138 tests pass, 0 failures, 0 errors.

## Session Wrap-up
- Generated official docs: Markdown (22KB), Postman collection (15KB), PDF (518KB)
- Updated dev-memory skill: auto-generates docs + Postman + PDF on module completion
- Fixed: ProjectMemberRepository property path (role → roleInProject)
- Fixed: permissions.action CHECK constraint needs CREDENTIAL_VIEW/CREDENTIAL_REVEAL (SQL migration created)
- Made DataInitializer resilient: gracefully skips permissions that fail DB constraint
