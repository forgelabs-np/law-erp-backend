# 2026-08-22

## NEW FEATURE: Hearing reminder emails (T-1) — 2026-08-23 (approved design: docs/superpowers/specs/2026-08-23-hearing-reminder-emails-design.md)
- HearingReminderScheduler (cron 0 0 8 * * *) → HearingReminderServiceImpl.sendRemindersForDate(tomorrow)
- Selects PESHI/SCHEDULED events for tomorrow; recipients = attendingAdvocateId → user email + our-client parties (clientId → user email, deduped)
- EmailService.sendHearingReminder (@Async, Thymeleaf email/hearing-reminder.html, per-firm SMTP resolution, recipient-specific subject/CTA, EMAIL_SENT/FAILED audit with recipient userId as trigger — AuditLog.userId NOT NULL)
- hearing_reminder_log table (unique courtEventId+recipientType+recipientEmail+scheduledDate) = idempotency: claim row before dispatch, async failure flips to FAILED
- NOTE: MatterPartyRepository.findByMatterIdInAndFirmIdAndOurClientTrue needed explicit @Query — Spring Data derived "ourClient" from getter but JPA attribute is isOurClient (field name) → UnknownPathException
- Verified live (E2EFIRM): PESHI 2026-08-24 15:00 → first run dispatched 2 (advocate+client), second run 0 (idempotent), EMAIL_FAILED auth error on dev Gmail (expected, no SMTP creds), log rows FAILED as designed
- +9 tests (HearingReminderServiceImplTest); full suite 177 pass

## FIX APPLIED: MEDIUM bugs (parties, timeline, ceiling, 405) — 2026-08-23
- Parties: `@NotBlank` → `@NotNull` on enum `roleType` in PartyEntryRequest + PartyRoleRequest (was HV000030 crash on every party add)
- Firm timeline: MatterTimelineRepository JPQL `(:from is null or ...)` → 42P18; fix = sentinel window bounds (1900/2999) from MatterServiceImpl.getFirmTimeline, IS NULL probes kept only on enums. NOTE: JPQL `CAST(:from AS timestamp)` did NOT work (Hibernate 7 binds the param as bytea → "cannot cast bytea to timestamp")
- Role-permission ceiling: replaced static scope mapping (ADVOCATE→ASSIGNED etc., rejected all TENANT-seeded perms) with parent-system-role permission-set membership check + GLOBAL-scope guard. Both FirmRoleServiceImpl and RolePermissionServiceImpl. Removed getMaxScopeForParent/isScopeAllowed
- 405/404: GlobalExceptionHandler now maps HttpRequestMethodNotSupportedException → 405 and NoResourceFoundException → 404 (Spring 7 ctor: (HttpMethod, String, String))
- Verified live: party add 200, matter+parties 200, timeline 200 (plain/from/to/type), role PUT same set 200 (was 403), outside-ceiling perm 403, wrong method 405, unknown path 404
- Tests: RolePermissionServiceTest rewritten (parent-set contract), +FirmRoleServiceImplTest(4), +GlobalExceptionHandlerTest(2), +PartyEntryRequestValidationTest(4). Full suite 168 pass
- Minor note: RolePermissionRequest.roleId @NotNull redundant with path (validated before controller sets it) — UI sends it, harmless

## FIX APPLIED: CRITICAL authz gaps (user request)
- Wired PermissionEvaluator.require() (existed but never called) into 9 controllers
- Project mgmt: all 5 controllers — class @PreAuthorize (SUPER_ADMIN/FIRM_ADMIN/ADVOCATE/PARALEGAL, blocks CLIENT) + per-endpoint PROJECT_MANAGEMENT:CREATE/VIEW/EDIT/DELETE/CREDENTIAL_VIEW/CREDENTIAL_REVEAL
- Case mgmt: Matter/CourtEvent/CaseAssignment/CourtCase controllers — CASE_MANAGEMENT:CREATE/EDIT/DELETE/VIEW
- Verified live: client reveal/credential access/create -> 403; paralegal create matter -> 403; advocate create project -> 403, create matter -> 200; firm admin all -> 200; client portal -> 200
- +16 tests (ProjectManagementAuthzTest, CaseManagementAuthzTest); full suite 157 pass
- NOTE: super admin on /projects -> 403 "Firm context required" is PRE-EXISTING service behavior (no firm context), not a regression
- Report updated: docs/test-report-2026-08-23.md §3.12

## E2E TESTING SESSION RESULTS (report: docs/test-report-2026-08-23.md)
- Full E2E across all modules/roles/3 firms; super admin aac1ss + TLAW testfirm (user creds) used to create E2EFIRM + team
- CRITICAL: /api/v1/projects/** has NO authz — client can create/list/update projects, reveal plaintext credentials, delete credentials, complete renewals (only firm-scoping 404 protects cross-tenant)
- CRITICAL: RBAC permission layer never enforced — PermissionEvaluator only clears cache; no hasPermission checks anywhere; PARALEGAL (READ_ONLY) can create matters
- MEDIUM bugs: parties broken (@NotBlank on PartyType enum → HV000030); firm timeline 500 (Postgres 42P18 param type); role-permission PUT broken for ADVOCATE/PARALEGAL/CLIENT (TENANT scope vs ASSIGNED/OWN ceiling); wrong-method → 500 not 405; matter list unscoped for advocate (dashboard IS scoped); no project DELETE endpoint
- Email: per-firm SMTP config works (save/mask/test); welcome+reset emails trigger but fail on dev Gmail (no password); NO hearing-reminder email feature exists (required for go-live)
- SystemConfig (global + per-firm PUT/GET/DELETE) verified working — planned home for registration secret
- Go-live TODOs logged in report §5: JWT→env, AES key→env, registration secret→SystemConfig, remove MFA bypass, enforce RBAC, hearing reminder emails, SMTP per firm, fix listed bugs
- Cross-tenant isolation PASS (404/403 across firms), SQLi PASS (parameterized), token tampering PASS

## Strix REMOVED (user decision) — full E2E testing session instead
- User: delete the Strix skill + everything; not needed now. Removed .agents/skills/security/, strix_runs/, ~/.strix CLI, killed processes, deleted token files
- New task: rigorous E2E testing of the whole app before going live (3 firms, all roles, permissions, cross-tenant isolation, data correctness, email reminders) → detailed Test Report + go-live TODO list
- App restarted with fresh log at /tmp/e2e-app.log

## DECISION (user, 2026-08-22): test-mode security items are INTENTIONAL — do NOT fix yet
- MFA bypass 123456 (TotpUtil DEV_BYPASS_CODE), committed JWT secret, committed AES config key, permitAll super-admin registration + committed secret
- User: "these are for testing right now, we will set this up later" — keep as-is; revisit when prod config is being set up

## Security testing session (app on :6969, dev profile)
- User wanted a Strix pentest, then manual testing; did BOTH (Strix in background, manual curl tests in parallel)
- Created test tenants in dev DB: SECFIRMA/SECFIRMB (admins secfirma_admin/secfirmb_admin), TLAW employees (tlaw_tlawadvocate, tlaw_tlawparalegal), clients (secclient_a, tlaw_client), matter SECFIRMA-MAT-2026-00001, project SECFIRMA-PRJ-2026-00001 + credential
- Registered super admin `sectestadmin` (was none in dev DB) — registration secret from application-dev.yml works, permitAll endpoint
- MFA flows: super admins + firm admins require TOTP; dev bypass code 123456 (see findings)
- Usernames for employees get firm-prefixed (UsernameGenerator): tlaw_tlawadvocate not tlaw_advocate
- Strix 1.5.3 installed to ~/.strix/bin, TokenRouter (api.tokenrouter.com) LLM via OPENAI_API_BASE; scan ran but stalled at turn ~20 — free-tier reasoning model too slow (~50K tokens/request)

## Security findings (manual testing, 2026-08-22)
- **CRITICAL: MFA bypass** — TotpUtil DEV_BYPASS_CODE "123456" passes whenever app.production != true; app.production NOT set in any yml (defaults false) → MFA is fake in current config
- **HIGH: JWT HS512 secret committed** in application-dev.yml — forge tokens for any user
- **HIGH: AES config-encryption key committed** (config.encryption.key default in application.yml) — decrypts stored portal credentials
- **MEDIUM-HIGH: super-admin registration permitAll + secret committed** — backdoor on fresh deployments (only one super admin allowed, so blocked once one exists)
- **MEDIUM: wrong HTTP method → 500 instead of 405** (GlobalExceptionHandler misses HttpRequestMethodNotSupportedException) — GET /super-admin/firms, GET /firm/matters/{no}/court-cases & /parties
- **LOW: super admin 403 on scraper endpoints** (hasAnyRole('FIRM_ADMIN') excludes SUPER_ADMIN)
- **LOW/INFO: misleading refresh error message; 24h access tokens; min-6-char password policy**
- Clean: cross-tenant BOLA (403/404/scoped), function-level authz (403), SQLi (parameterized, no timing), JWT tampering (401), refresh-token abuse (401), unauth (401), mass assignment (DTOs clean), client portal scoping

## Strix security skills installed — manual-only policy
- Installed all 9 skills from `usestrix/strix` via `npx skills add usestrix/strix --yes`
- User mandate: **never run them automatically** — only on explicit request in the same message
- Moved all 9 into `.agents/skills/security/` (nested folder) — the skill loader scans only one level deep, so they are NOT auto-discoverable and won't show up in available-skills lists; that's intentional and matches the "never auto-run" rule
- To use one: read `.agents/skills/security/<name>/SKILL.md` directly from disk and follow it
- Added `.agents/skills/security/README.md` documenting the manual-only policy
- Only ever run them against the user's own app/staging, on explicit request (they perform real exploit attempts)
