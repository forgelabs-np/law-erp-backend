# 2026-09-06

## Scraper module — finished the half-broken session's work
- Fixed WIP compile errors: `HearingStatusStatusResponse` typo in `ScraperService`, missing imports (`Optional`, `PathVariable`), `responseHandler.notFound(...)` doesn't exist → threw `ResourceNotFoundException` instead (codebase 404 idiom, handled by `GlobalExceptionHandler`)
- `CourtRepository` derived queries referenced nonexistent properties (`courtNameNp`, `id`, then `courtName`) — each killed the whole Spring context at startup (E2E tests fail with opaque "ApplicationContext failure threshold" errors; grep surefire reports for "Caused by:" to find the real one)
- Lombok gotchas hit: boolean `notified` → `isNotified()` not `getNotified()`; `Court` `@AllArgsConstructor` takes 4 args incl. `isActive`
- Replaced fabricated seed data (garbled placeholder high courts, "IDs TBD") by scraping the real homepage HTML of supremecourt.gov.np: 77 district courts with exact id↔name mapping (`/weekly_dainik/pesi/daily/<id>` links). High courts live on a separate `appeal/syspublic.php` system the scraper client can't talk to → deliberately NOT seeded
- Verified districts count is 77, not 86 (nav has ~79 labels but 77 usable pesi routes; 93/94 don't exist)
- `getActiveCourts()` = ACTIVE client cases ∩ registry `isActive=true` (kill-switch semantics documented on `Court.isActive`); tests stub both repo sides
- `/cases/{caseNo}/matches` now ordered by `hearingDateAd DESC` to match its own doc

## Court names in English + pattern refactor (user request)
- `Court` entity: `courtName` → `courtNameNepali` (`court_name_nepali`) + `courtNameEnglish` (`court_name_english`); columns left nullable so `ddl-auto: update` can't fail on existing tables (seeder guarantees values). Added `@Builder`
- New `CourtSeeder` collaborator in scraper service package (project pattern: interface+Impl + single-responsibility `@Service` collaborators like `CourtCaseRefGenerator`). Upsert-by-courtId at startup: inserts missing, refreshes names, never touches `courtType`/`isActive` → operator kill-switch survives redeploys, self-heals rows with NULL names after the column rename
- Removed inline seed method from `ScraperServiceImpl` (~100 lines)
- `ScraperMapper.resolveCourtName` → `resolveCourtNameNepali` + `resolveCourtNameEnglish`; `HearingStatusResponse` carries both; `CourtDto` exposes both
- 245 tests green, BUILD SUCCESS

## Gotchas for next time
- Test-project root here is `law-erp-backend/` inside `G:/Projects/LAW-ERP` — the project root has the frontend too; repo diff paths are backend-relative
- Spring Data derived-query property names must match entity fields exactly; typo = context-load failure, not a compile error
