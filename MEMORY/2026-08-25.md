# 2026-08-25 — Invoice Module + Query Optimization + Dashboard Trends

## Query Optimization (6 hotspots fixed)
- **GlobalDashboardServiceImpl** — full-table scans (findAll + stream.count) → pure COUNT SQL queries
- **RoleManagementServiceImpl** — N+1 permission loading per role → batch `findByRoleIdIn`
- **PermissionServiceImpl** — N+1 in grouped listing → batch `findByModuleIdIn`; Java filter → SQL `findAllActive`
- **PermissionEvaluator** — 2 DB hits per request → zero (JWT fast-path)
- 7 new optimized repository methods (countByFirmId, countByStatus, countByFirmIdAndStatus, etc.)

## Dashboard Trends (new feature)
- `GET /api/v1/modules/dashboard?days=7|30|90|180|365`
- Returns `userTrends`, `matterTrends`, `firmTrends` with cumulative daily data
- Single GROUP BY DATE(createdAt) query per entity type — no full-table scan
- Firm-scoped for FIRM_ADMIN, all-firms for SUPER_ADMIN

## Invoice Generator Module (new module — SUPER_ADMIN only)
- **Entities:** Invoice header + InvoiceItem line items
- **Status flow:** DRAFT → SENT → PAID/OVERDUE → CANCELED
- **8 API endpoints:** CRUD + status transitions + PDF download + email send
- **PDF generation:** Thymeleaf HTML template → OpenHTMLtoPDF (openhtmltopdf-core + openhtmltopdf-pdfbox 1.0.10) → PDF bytes
- **Email:** PDF attached to email via EmailServiceImpl
- **Module path:** `com.lawfirm.erp.modules.invoice`
- **Design spec:** `docs/invoice-generator-design.md`
- **Template:** `src/main/resources/templates/invoice-template.html`
- **Email template:** `src/main/resources/templates/email/invoice.html`

## Files Created
- `modules/invoice/enums/InvoiceStatus.java`
- `modules/invoice/entity/Invoice.java`, `InvoiceItem.java`
- `modules/invoice/repository/InvoiceRepository.java`, `InvoiceItemRepository.java`
- `modules/invoice/dto/request/` — CreateInvoiceRequest, UpdateInvoiceRequest, UpdateInvoiceStatusRequest, InvoiceItemRequest
- `modules/invoice/dto/response/` — InvoiceResponse, InvoiceDetailResponse
- `modules/invoice/mapper/InvoiceMapper.java`
- `modules/invoice/service/InvoiceService.java`, `InvoiceServiceImpl.java`, `InvoicePdfService.java`
- `modules/invoice/controller/InvoiceController.java`
- `templates/invoice-template.html`
- `templates/email/invoice.html`

## Dependency Added
- `openhtmltopdf-core` 1.0.10 (fixes "Cannot resolve method 'withHtmlContent'" — class lives in core, not pdfbox)

## Tests: 178 pass, BUILD SUCCESS
