# 2026-08-28

## Menu Management module added
- Added `MENU_MANAGEMENT` module in DataInitializer (order 12, sort 120, icon `MenuIcon`, path `/menus`)
- Standard permissions seeded: ACCESS, VIEW, CREATE, EDIT, DELETE
- Role matrix: SUPER_ADMIN + FIRM_ADMIN = FULL, everyone else = NO_ACCESS
- ModuleController refactored: removed `@PreAuthorize("hasRole('SUPER_ADMIN')")`, replaced with fine-grained `permissionEvaluator.require("MENU_MANAGEMENT:...")` per endpoint
  - CREATE → upsert, VIEW → list/get/toggle-active, EDIT → toggle + assign-permissions, DELETE → delete

## Permission Management module added
- Added `PERMISSION_MANAGEMENT` module in DataInitializer (order 13, sort 130, icon `KeyIcon`, path `/permissions`)
- Standard permissions seeded: ACCESS, VIEW, CREATE, EDIT, DELETE
- Role matrix: SUPER_ADMIN + FIRM_ADMIN = FULL, everyone else = NO_ACCESS
- PermissionController refactored: replaced all `ROLE_MANAGEMENT:...` checks with `PERMISSION_MANAGEMENT:...`
  - CREATE → upsert, VIEW → list/grouped/active/findById, EDIT → toggle, DELETE → delete

## Scraper Management module added
- Added `SCRAPER_MANAGEMENT` module in DataInitializer (order 14, sort 140, icon `GlobeIcon`, path `/scraper`)
- Extra action: `EXPORT` (beyond standard ACCESS, VIEW, CREATE, EDIT, DELETE)
- Role matrix: SUPER_ADMIN + FIRM_ADMIN = FULL, everyone else = NO_ACCESS
- ScraperAdminController refactored: added `PermissionEvaluator` injection + permission checks
  - `SCRAPER_MANAGEMENT:CREATE` → scrape endpoint
  - `SCRAPER_MANAGEMENT:EXPORT` → export endpoint

## User Management module added
- Added `USER_MANAGEMENT` module in DataInitializer (order 15, sort 150, icon `UsersIcon`, path `/users`)
- Standard permissions seeded: ACCESS, VIEW, CREATE, EDIT, DELETE
- Role matrix: SUPER_ADMIN + FIRM_ADMIN = FULL, everyone else = NO_ACCESS
- UserManagementController refactored: replaced all `EMPLOYEE:...` checks with `USER_MANAGEMENT:...`
  - VIEW → list, search, profile, permissions, activity
  - EDIT → reset-password, bulk-role-change
  - DELETE → bulk-deactivate
- Zero `EMPLOYEE:` references remain in main source code

## Dashboard Management module added
- Added `DASHBOARD_MANAGEMENT` module in DataInitializer (order 16, sort 160, icon `LayoutDashboardIcon`, path `/dashboard`)
- Standard permissions seeded: ACCESS, VIEW, CREATE, EDIT, DELETE
- Role matrix: SUPER_ADMIN + FIRM_ADMIN = FULL, ADVOCATE + PARALEGAL = READ_ONLY, CLIENT = NO_ACCESS
- Updated 3 dashboard controllers:
  - GlobalDashboardController: added `permissionEvaluator` injection + `DASHBOARD_MANAGEMENT:VIEW`
  - DashboardController (firm): replaced `CASE_MANAGEMENT:VIEW` with `DASHBOARD_MANAGEMENT:VIEW`
  - ProjectDashboardController: replaced `PROJECT_MANAGEMENT:VIEW` with `DASHBOARD_MANAGEMENT:VIEW`
