package com.lawfirm.erp.modules.notification.enums;

/** Out-of-band delivery channels tracked in notification_delivery. IN_APP is the notification row itself, not tracked here. */
public enum DeliveryChannel {
    EMAIL,
    SMS,   // future
    PUSH   // future
}
