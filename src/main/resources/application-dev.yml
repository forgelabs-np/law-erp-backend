spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
    username: postgres.fpqmxvcdglizgdymrelw
    password: "@Ashishthapa46"
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 1200000
      pool-name: NepalCRM-HikariPool

  jpa:
    database: POSTGRESQL
    database-platform: org.hibernate.dialect.PostgreSQLDialect
#    show-sql: true
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#        format_sql: true

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    # Gmail needs an App Password (16 chars, no spaces). Do not paste anything after
    # the closing brace — the whole line becomes the username and SMTP auth fails.
    username: ${MAIL_USER:tryaac1ss@gmail.com}
    password: ${MAIL_PASS:acxffrwibjfoibas}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: YnBPN15lvxCkxOAAe5duFQ4MYVLf6B2e8AIkND8L35kRO7ByvtLrJQ1YKtLYbR6me0FysLcvtq8UFKAWiQyKeT
  access-expiry: 86400000
  refresh-expiry: 604800000
  token-expiry: 86400000

super-admin:
  registration-secret: "cdae9adca9e7cb0ab2afb82e2a2a74b7"

# Boot fallback only — the GLOBAL APP_PRODUCTION config key wins once the DB is seeded.
# Letting this yml value win in dev would re-enable the TotpUtil "123456" MFA bypass.
app:
  name: Tarikh
  production: false
